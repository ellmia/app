# Git Workflow

- Always run `npm run check` before committing
- Use conventional commits if possible
- For symlinks and agent setup: `ln -s` is allowed (see permissions in settings)
- Before deploy: ensure clean working tree and successful build

Common commands allowed:
- git status, diff, log, add, commit, push
- ln -s (for recreating agent symlinks if needed)